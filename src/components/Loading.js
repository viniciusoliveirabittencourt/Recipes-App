import React from 'react';
import loading from '../images/loading.gif';

function Loading() {
  return (
    <div>
      <img src={ loading } id="teste" alt="loading" />
    </div>
  );
}

export default Loading;
